<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>DID Workshop | ABT Chain Node</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet" />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
  </head>
  <body>
    <nav class="nav">
      <div class="nav-wrapper container">
        <a href="/" class="brand-logo">ABT DID Workshop</a>
        <ul class="right hide-on-med-and-down">
          <li class="menu menu-app"><a href="/">Application</a></li>
          <li class="menu menu-demo"><a href="/demo">Demo Case</a></li>
        </ul>
      </div>
    </nav>

    <main id="main" role="main" class="container">
      <div class="row">
        <div class="col s12">
          <%= if get_flash(@conn, :info) != nil do %>
            <p class="alert alert-info" role="alert"><i class="material-icons">error</i><%= get_flash(@conn, :info) %></p>
          <% end %>
          <%= if get_flash(@conn, :warning) != nil do %>
            <p class="alert alert-warning" role="alert"><i class="material-icons">warning</i><%= get_flash(@conn, :warning) %></p>
          <% end %>
          <%= if get_flash(@conn, :error) != nil do %>
            <p class="alert alert-danger" role="alert"><i class="material-icons">info</i><%= get_flash(@conn, :error) %></p>
          <% end %>

          <%= render @view_module, @view_template, assigns %>
        </div>
      </div>
    </main>

    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
    <script>
      $(document).ready(function () {
        var pathname = window.location.pathname;
        if (pathname.indexOf('/demo') === 0 || pathname.indexOf('/tx') === 0) {
          $('.nav .menu-demo').addClass('active');
        } else {
          $('.nav .menu-app').addClass('active');
        }
        $('select').formSelect();
        $('.collapsible').collapsible();
        $('.tooltipped').tooltip();
      });
    </script>
    <%= Drab.Client.run(@conn) %>
  </body>
</html>
