<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>DID Workshop | ABT Chain Node</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet" />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
  </head>
  <body>
    <nav class="light-blue">
      <div class="nav-wrapper container">
        <a href="/" class="brand-logo">
          ABT DID Workshop
        </a>
      </div>
    </nav>

    <main id="main" role="main" class="container">
      <div class="row">
        <div class="col s12">
          <%= if @conn.assigns[:error] != nil do %>
            <div class="alert alert-danger"><i class="material-icons">error</i><%= @conn.assigns[:error] %></div>
          <% end %>
          <%= if @conn.assigns[:warning] != nil do %>
            <div class="alert alert-warning"><i class="material-icons">warning</i><%= @conn.assigns[:warning] %></div>
          <% end %>
          <%= if @conn.assigns[:info] != nil do %>
            <div class="alert alert-info"><i class="material-icons">info</i><%= @conn.assigns[:info] %></div>
          <% end %>

          <%= if get_flash(@conn, :info) != nil do %>
            <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
          <% end %>
          <%= if get_flash(@conn, :error) != nil do %>
            <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
          <% end %>

          <%= render @view_module, @view_template, assigns %>
        </div>
      </div>
    </main>

    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
    <script>
      $(document).ready(function () {
        $('select').formSelect();
      });
      $(document).ready(function(){
        $('.collapsible').collapsible();
      });
    </script>
    <%= Drab.Client.run(@conn) %>
  </body>
</html>
